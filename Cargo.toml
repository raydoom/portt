[package]
name = "portt"
version = "0.2.0"
edition = "2021"
description = "A ping-like tool for TCP/UDP ports"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
ctrlc = "3"

[profile.release]
opt-level = 3
